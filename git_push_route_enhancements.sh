#!/bin/bash

# 路线计算和布局改进提交脚本
echo "🚀 开始提交路线计算和布局改进..."

# 添加所有更改
git add .

# 提交更改
git commit -m "路线计算功能增强和布局优化

功能改进:
- 路线计算按钮现在会自动刷新数据，确保使用最新的订单信息
- 计算路线前先同步飞书数据，再重新加载地图数据
- 添加数据同步错误处理，失败时使用现有数据继续计算

布局优化:
- 统计看板位置下移至80px，避免与地图控件冲突
- 位置定位按钮调整为32x32尺寸，与图层按钮保持一致
- 位置定位按钮移至图层按钮下方(130px)，避免遮挡
- 更新移动端响应式设计，确保所有元素正确显示
- 优化按钮间距和视觉层次"

# 推送到远程仓库
git push origin main

echo "✅ 路线计算和布局改进已成功推送到GitHub!"
echo "�� Netlify将自动部署更新..." 