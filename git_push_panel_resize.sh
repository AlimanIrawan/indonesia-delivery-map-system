#!/bin/bash

# 面板尺寸调整提交脚本
echo "📏 开始提交面板尺寸调整..."

# 添加所有更改
git add .

# 提交更改
git commit -m "左侧面板尺寸优化：缩小到原来的1/3宽度

界面优化:
- 统计面板宽度从160px缩小到55px
- 路线优化面板同样缩小到55px
- 调整内边距和字体大小以适应更小的空间
- 路线优化按钮改为垂直排列节省空间

样式调整:
- 统计项字体从11px调整到10px/9px
- 按钮内边距和字体相应缩小
- 图例和统计信息居中对齐
- 保持功能完整性的同时最大化地图显示区域

响应式设计:
- 移动端面板宽度调整到50-55px
- 确保在各种屏幕尺寸下正确显示
- 优化触摸设备的可用性"

# 推送到远程仓库
git push origin main

echo "✅ 面板尺寸调整已成功推送到GitHub!"
echo "�� Netlify将自动部署更新..." 